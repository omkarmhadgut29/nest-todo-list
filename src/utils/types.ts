export type CreateTodoType = {
  // id: string;
  title: string;
  description: string;
};

export type User = {
  // id: string;
  password: string;
  username: string;
  fullname: string;
  email: string;
};
